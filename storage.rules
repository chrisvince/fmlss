rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /post-assets/{paths=**} {
      allow read;
    }
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
